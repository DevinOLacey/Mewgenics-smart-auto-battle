// Butcher abilities AI patches
// All abilities should have explicit ai_base_score for proper AI prioritization

// ===== MOVEMENT ABILITIES =====

HogRush.merge { damage_instance { ai_base_score 0 } }
HogRush2.merge { damage_instance { ai_base_score 0 } }

Trudge.merge { damage_instance { ai_base_score 0 } }
Trudge2.merge { damage_instance { ai_base_score 0 } }

CannonBall.merge { damage_instance { ai_base_score 0 } }
CannonBall2.merge { damage_instance { ai_base_score 0 } }

// ===== ATTACK ABILITIES =====

Burp.merge { damage_instance { ai_base_score 0 } }
Burp2.merge { damage_instance { ai_base_score 0 } }

ForceFeed.merge { damage_instance { ai_base_score 0 } }
ForceFeed2.merge { damage_instance { ai_base_score 0 } }

Fartoom.merge { damage_instance { ai_base_score -10 } }  // Self-damage 25% HP
Fartoom2.merge { damage_instance { ai_base_score -10 } }

Mutilate.merge { damage_instance { ai_base_score 0 } }
Mutilate2.merge { damage_instance { ai_base_score 0 } }

SkullBash.merge { damage_instance { ai_base_score -20 } }  // SelfStun
SkullBash2.merge { damage_instance { ai_base_score -10 } }  // 50% SelfStun chance

Shred.merge { damage_instance { ai_base_score 0 } }
Shred2.merge { damage_instance { ai_base_score 0 } }

Chomp.merge { damage_instance { ai_base_score 0 } }  // Leech + ally buff
Chomp2.merge { damage_instance { ai_base_score 0 } }

Succ.merge { damage_instance { ai_base_score -5 } }  // Collects pickups, not combat
Succ2.merge { damage_instance { ai_base_score -5 } }

Consume.merge { damage_instance { ai_base_score -20 } }  // Self-damage Drowsy (sleep)
Consume2.merge { damage_instance { ai_base_score -20 } }

BodySlam.merge { damage_instance { ai_base_score -5 } }  // Self-damage 25% HP
BodySlam2.merge { damage_instance { ai_base_score -5 } }

BloodMagic.merge { damage_instance { ai_base_score 0 } }  // Self-damage for mana, useful
BloodMagic2.merge { damage_instance { ai_base_score 0 } }

SmellBlood.merge { damage_instance { ai_base_score 0 } }  // Self-buff, useful
SmellBlood2.merge { damage_instance { ai_base_score 0 } }

Vurp.merge { damage_instance { ai_base_score 0 } }  // Once per fight, requires trinket
Vurp2.merge { damage_instance { ai_base_score 0 } }

SliceAndDice.merge { damage_instance { ai_base_score 0 } }
SliceAndDice2.merge { damage_instance { ai_base_score 0 } }

LunchTime.merge { damage_instance { ai_base_score 0 } }  // Targets food, useful
LunchTime2.merge { damage_instance { ai_base_score 0 } }

// Tromp: self-damage Bruise to self - slight penalty
Tromp.merge { damage_instance { ai_base_score -5 } }
Tromp2.merge { damage_instance { ai_base_score -5 } }

LightenTheLoad.merge { damage_instance { ai_base_score -10 } }  // Self-damage 25 HP for turn
LightenTheLoad2.merge { damage_instance { ai_base_score -10 } }

Crushinator.merge { damage_instance { ai_base_score 0 } }
Crushinator2.merge { damage_instance { ai_base_score 0 } }

Monch.merge { damage_instance { ai_base_score 0 } }  // Damage scales with HP, useful
Monch2.merge { damage_instance { ai_base_score 0 } }

DeathWind.merge { damage_instance { ai_base_score 0 } }
DeathWind2.merge { damage_instance { ai_base_score 0 } }

Spoil.merge { damage_instance { ai_base_score -5 } }  // Targets food, situational
Spoil2.merge { damage_instance { ai_base_score -5 } }

Grill.merge { damage_instance { ai_base_score 0 } }  // Targets food, useful
Grill2.merge { damage_instance { ai_base_score 0 } }

Roast.merge { damage_instance { ai_base_score 0 } }
Roast2.merge { damage_instance { ai_base_score 0 } }

BadGas.merge { damage_instance { ai_base_score -5 } }  // Self-knockback, self-damage risk
BadGas2.merge { damage_instance { ai_base_score -5 } }

ButcherPurge.merge { damage_instance { ai_base_score 0 } }  // Self-debuff CON -1, useful utility
ButcherPurge2.merge { damage_instance { ai_base_score 0 } }

Binge.merge { damage_instance { ai_base_score -5 } }  // Self-debuff Speed -1
Binge2.merge { damage_instance { ai_base_score -5 } }

MyTurn.merge { damage_instance { ai_base_score -20 } }  // Steals ally turn
MyTurn2.merge { damage_instance { ai_base_score -20 } }

Gib.merge { damage_instance { ai_base_score 0 } }  // Spawns food, useful
Gib2.merge { damage_instance { ai_base_score 0 } }

Swallow.merge { damage_instance { ai_base_score 0 } }  // Collects pickups, useful
Swallow2.merge { damage_instance { ai_base_score 0 } }

Track.merge { damage_instance { ai_base_score -5 } }  // Move toward food, situational
Track2.merge { damage_instance { ai_base_score -5 } }

Sharpen.merge { damage_instance { ai_base_score 0 } }  // Weapon buff, useful
Sharpen2.merge { damage_instance { ai_base_score 0 } }

FireFart.merge { damage_instance { ai_base_score 0 } }
FireFart2.merge { damage_instance { ai_base_score 0 } }

RoughToss.merge { damage_instance { ai_base_score 0 } }
RoughToss2.merge { damage_instance { ai_base_score 0 } }

Bowl.merge { damage_instance { ai_base_score 0 } }  // Targets bowling ball, situational
Bowl2.merge { damage_instance { ai_base_score 0 } }

BowlDash.merge { damage_instance { ai_base_score 0 } }  // Utility
BowlDash2.merge { damage_instance { ai_base_score 0 } }  // Utility

TaintedOffering.merge { damage_instance { ai_base_score -99999 } }  // Self-sleep, dangerous
TaintedOffering2.merge { damage_instance { ai_base_score 0 } }  // No self-sleep, useful

Tryptophan.merge { damage_instance { ai_base_score -99999 } }  // AoE Sleep hits allies
Tryptophan2.merge { damage_instance { ai_base_score -20 } }  // Sleep + heal self, still risky

Chonkwalk.merge { damage_instance { ai_base_score -5 } }  // Self-damage 5 for movement
Chonkwalk2.merge { damage_instance { ai_base_score -5 } }

Reflux.merge { damage_instance { ai_base_score -5 } }  // Self-damage Burn 2
Reflux2.merge { damage_instance { ai_base_score -5 } }

Cough.merge { damage_instance { ai_base_score -5 } }  // Self-damage 2 to spawn RotFly
Cough2.merge { damage_instance { ai_base_score -5 } }

Butcher.merge { damage_instance { ai_base_score -20 } }  // AoE instakill can kill allies
Butcher2.merge { damage_instance { ai_base_score -20 } }

// SelfMutilate: self-damage for StrengthUp+Cleave - core Butcher mechanic
SelfMutilate.merge { damage_instance { ai_base_score -5 } }
SelfMutilate2.merge { damage_instance { ai_base_score -5 } }
